import React from 'react'
import ReactDOM from 'react-dom/client'
import CampusSync from './CampusSync.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <CampusSync />
    </React.StrictMode>,
)
